// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://amnaqureshi1.github.io',
  base: '/doane_portfolio',
  trailingSlash: 'always',
});
